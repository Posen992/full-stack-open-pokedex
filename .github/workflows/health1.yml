name: Health check1

on:
  schedule:
    - cron: '30 12 * * *'
  workflow_dispatch:

jobs:
  health_check:
    runs-on: ubuntu-20.04
    steps:
      - name: Check Health
        run: curl https://full-stack-open-pokedex-urty.onrender.com/health